<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.kosmo.today.mapper.WishMapper">

	<resultMap id="faqList" type="edu.kosmo.today.vo.FaqVO">
		<result property="bid" column="bid" />
		<result property="btitle" column="btitle" />
		<result property="bcontent" column="bcontent" />
		<result property="bdate" column="bdate" />
		<result property="mnum" column="mnum" />
		<result property="tnum" column="tnum" />
	</resultMap>
	
	<select id="faqList" resultMap="faqList">
	<![CDATA[
 		          SELECT * FROM (
              SELECT ROWNUM AS RNUM, A.* FROM (
                 SELECT  * FROM boardlist WHERE tnum = 3
             ) A WHERE ROWNUM <= #{pageNum} * #{amount}
         ) WHERE RNUM > (#{pageNum}-1) * #{amount}
			
	]]>
	</select>
	
	<select id="getTotal" resultType = "int">
	<![CDATA[
		select count(*) from boardlist where tnum = 3
	]]>
	</select>

</mapper>
