<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<!-- namespace는 패키지 명 + 인터페이스명과 맞춰줘야한다. -->
<mapper namespace="edu.kosmo.today.mapper.MemberMapper">
  			
   <select id="getList" resultType="edu.kosmo.today.vo.MemberVO">
   <![CDATA[
      select mnum, memail, mnickname from member order by mnum
   ]]>
   </select>  
   
   <select id="read" resultType="edu.kosmo.today.vo.MemberVO">
	<![CDATA[
		select * from member where mnum = #{mnum}
	]]>
	</select>
	
	<delete id="delete" >
	<![CDATA[
		delete from member where mnum = #{mnum}
	]]>
	</delete>
	

	<select id="getTotalCount" resultType = "int">
	<![CDATA[
		select count(*) from member
	]]>
	</select>
	
	<!-- 	열개씩 뽑아내는거 한페이지에 출력해주는 역할 -->
	<select id="getListWithPaging" resultType = "edu.kosmo.today.vo.MemberVO">
	<![CDATA[
		         SELECT * FROM (
              SELECT ROWNUM AS RNUM, A.* FROM (
                       SELECT
                             *
                         FROM
                             member 
                         order by mnum   
             ) A WHERE ROWNUM <= #{pageNum} * #{amount}
         ) WHERE RNUM > (#{pageNum}-1) * #{amount}
			
	]]>
	</select>
	
	

   
</mapper>